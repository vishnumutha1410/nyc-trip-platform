name: 'nyc_trip_platform'
version: '1.0.0'
config-version: 2

profile: 'nyc_trip_platform'

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]

models:
  nyc_trip_platform:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts

seeds:
  nyc_trip_platform:
    taxi_zone_lookup:
      +schema: raw
      +column_types:
        LocationID: number
